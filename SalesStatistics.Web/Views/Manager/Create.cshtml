@model SalesStatistics.Web.Models.ViewModels.ManagerViewModel

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Manager</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        @Html.Partial("~/Views/Manager/_ManagerCreate.cshtml")

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input id="createButton" type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

    @*<script type="text/javascript">
            onCreateManager = function () {
                $("form").on("submit", function (e) {
                    e.preventDefault();
                    var url = '@Url.Action("Index", "Manager")';
                    window.location.href = url;
                });
            }

            $("#createButton").click(onCreateManager);
        </script>*@
}
