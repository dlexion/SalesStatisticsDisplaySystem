@model SalesStatistics.Web.Models.ViewModels.ManagerViewModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>ManagerViewModel</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        @Html.Partial("~/Views/Manager/_ManagerCreate.cshtml", Model)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input id="saveButton" type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

    @*<script type="text/javascript">
        onSaveManager = function() {
            $("form").on("submit", function (e) {
                e.preventDefault();
                $.post('@Url.Action("Edit", "Manager")', $("form").serialize(), function (data) {
                    alert("gg");
                    var url = '@Url.Action("Index", "Manager")';
                    //window.location.href = url;
                });
            });
        }
        $("#saveButton").click(onSaveManager);
    </script>*@
}
